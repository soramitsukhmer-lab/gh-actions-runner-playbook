---
- name: Install GitHub Runners
  hosts: all
  become: yes
  vars_files:
    - vars/aws.yml
    - vars/github.yml
  roles:
    - role: runner-installer
- name: Configure Docker and AWS Cli
  hosts: all
  vars_files:
    - vars/aws.yml
    - vars/github.yml
  roles:
    - role: aws-configure
    - role: docker-configure
    - role: runner-configure
